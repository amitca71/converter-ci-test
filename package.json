{
  "name": "vn-converter-sample",
  "version": "1.0.3",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sync_version": "bump-version",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"app/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "test": "mocha --reporter spec --exit; jest --config jestconfig.json; mocha --reporter mocha-junit-reporter --reporter-options mochaFile=reports/mocha/test-results.xml --exit; jest --ci --runInBand --reporters=default --reporters=jest-junit --config jestconfig.json",
    "prepare": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.4",
    "express": "^4.17.1",
    "jest-junit": "^12.2.0",
    "mocha": "^9.0.0",
    "mocha-junit-reporter": "^2.0.0",
    "npm-auto-bump-patch": "^1.0.1",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^27.0.4",
    "prettier": "^2.3.1",
    "ts-jest": "^27.0.3",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.3.4"
  }
}
